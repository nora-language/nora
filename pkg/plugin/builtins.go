package plugin

// RegisterBuiltinMacros registers the standard library macros as native Go fallbacks.
func (m *PluginManager) RegisterBuiltinMacros() {
	// Builtin macros are now exclusively handled by WASM plugins.
}
